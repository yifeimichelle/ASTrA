#include "frame.h"
#include "system.h"
#include "atom.h"
#include <vector>
#include "atomCounter.h"

AtomCounter::AtomCounter(System& a_system)
{
  m_system = a_system;
  // resize vectors
};

void AtomCounter::sample(Frame& a_frame)
{
  // Calculate COMs from the atom positions
}

void AtomCounter::normalize()
{
}
